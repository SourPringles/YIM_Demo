name: yim_demo
description: "yim_demo"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  image_picker: ^0.8.7+4
  ini: ^2.0.0
  shared_preferences: ^2.2.0
  path_provider: ^2.1.1
  provider: ^6.0.0
  workmanager: ^0.5.1
  flutter_local_notifications: ^16.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # 이 부분은 다음과 같이 구성할 수도 있습니다 (필요한 경우)
  # assets:
  #   - images/
  #   - icons/
